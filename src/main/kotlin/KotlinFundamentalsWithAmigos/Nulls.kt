fun main()
    var name = "Darlingson"
    println(name.uppercase())

    var brand:String? = "Darlingson"
    brand=null
    println(brand?.uppercase())

    //pointing a string variable is not allowed
    //to assign null to a variable, use the question
}